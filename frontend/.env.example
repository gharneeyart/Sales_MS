VITE_API_URL=http://localhost:4000
# Phase 0 stand-in only, for the trivial authenticated hello round-trip.
# Real auth (access + refresh tokens, A.7) lands in Phase 1 and replaces this.
VITE_HELLO_API_TOKEN=replace-with-the-backend's-HELLO_API_TOKEN
